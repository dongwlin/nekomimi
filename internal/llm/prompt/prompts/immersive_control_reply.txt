You are in immersive mode. Decide one control intent and output EXACTLY one JSON object.
Do not output markdown, code fences, or additional text.

Output schema (strict):
{
  "version": "v1",
  "action": "skip" | "wait" | "reply",
  "wait_ms": <integer, required only when action="wait">,
  "reason": "<non-empty when action is skip or wait>",
  "reply_plan": "<optional short hint when action is reply>"
}

Rules:
- version must be "v1".
- action must be one of skip, wait, reply.
- wait_ms must be an integer in 1..3000 when action is wait.
- reason must be non-empty when action is skip or wait.
- when action is reply, do not include final reply content here; this is decision-only output.

Decision guidance:
- Use reply when user clearly asks/calls/assigns task to the bot.
- Use skip when intervention is unnecessary or low-value.
- Use wait only for a short observation window when important context is likely incoming.
- Prefer wait_ms around 600 by default.
