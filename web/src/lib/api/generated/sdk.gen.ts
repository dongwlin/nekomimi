// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AuthLoginData, AuthLoginErrors, AuthLoginResponses, AuthPassphraseRotateData, AuthPassphraseRotateErrors, AuthPassphraseRotateResponses, AuthRefreshData, AuthRefreshErrors, AuthRefreshResponses, AuthVerifyData, AuthVerifyErrors, AuthVerifyResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Exchange passphrase for access/refresh token pair
 *
 * Validate the single passphrase and issue PASETO tokens.
 */
export const authLogin = <ThrowOnError extends boolean = false>(options: Options<AuthLoginData, ThrowOnError>) => (options.client ?? client).post<AuthLoginResponses, AuthLoginErrors, ThrowOnError>({
    url: '/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rotate system passphrase
 *
 * Rotate passphrase with current passphrase and revoke existing sessions immediately.
 */
export const authPassphraseRotate = <ThrowOnError extends boolean = false>(options: Options<AuthPassphraseRotateData, ThrowOnError>) => (options.client ?? client).post<AuthPassphraseRotateResponses, AuthPassphraseRotateErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }],
    url: '/auth/passphrase/rotate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Refresh access token with refresh token
 *
 * Rotate refresh token and issue a new token pair.
 */
export const authRefresh = <ThrowOnError extends boolean = false>(options: Options<AuthRefreshData, ThrowOnError>) => (options.client ?? client).post<AuthRefreshResponses, AuthRefreshErrors, ThrowOnError>({
    url: '/auth/refresh',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Verify access token
 *
 * Check whether the supplied Bearer access token is valid.
 */
export const authVerify = <ThrowOnError extends boolean = false>(options?: Options<AuthVerifyData, ThrowOnError>) => (options?.client ?? client).get<AuthVerifyResponses, AuthVerifyErrors, ThrowOnError>({
    security: [{ name: 'Authorization', type: 'apiKey' }],
    url: '/auth/verify',
    ...options
});
